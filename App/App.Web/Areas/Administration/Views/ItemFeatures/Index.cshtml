@model IEnumerable<App.Models.ViewModels.ItemFeatureViewModel>
@{
    ViewBag.Title = "Характеристики в продуктите";
}

<div class="row" style="margin-top:20px;">
    <div class="col-md-4">
        <h3>@ViewBag.Title</h3>
        <table class="table table-striped table-hover ">
            <thead>
                <tr>
                    <th>Характеристика</th>
                    <th style="text-align:center;"><img src="~/Areas/Administration/Content/img/cog-icon.png" /></th>
                </tr>
            </thead>
            <tbody>
                @Html.Partial("Partials/_ItemFeatures", Model)
            </tbody>
        </table>
    </div>
</div>
<div class="row">
    <div class="col-md-6">
        <a class="btn btn-primary" href="@Url.Action("Create")">Добави характеристика</a>
    </div>
</div>